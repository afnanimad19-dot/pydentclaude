// Feature flags. Flip these on once the backing integration is ready.

// The Patient-chart clinical modules (tooth chart, ledger, claims, prescriptions)
// stay hidden until the OpenDental API connection is available. Set to `true`
// to re-enable the sidebar group, the patient-profile tabs and the
// /dashboard/clinical/* pages.
export const CLINICAL_MODULES_ENABLED = false;
